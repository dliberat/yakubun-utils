const addMinutesToHours = require('./utils/addMinutesToHours');
const CheckResult = require('./utils/CheckResult');
const compareArrays = require('./utils/compareArrays');
const regexComparer = require('./utils/regexComparer');
const regexMatchesToArray = require('./utils/regexMatchesToArray');
const replaceAllFromArray = require('./utils/replaceAllFromArray');

module.exports = {
  addMinutesToHours,
  CheckResult,
  compareArrays,
  regexComparer,
  regexMatchesToArray,
  replaceAllFromArray,
};
