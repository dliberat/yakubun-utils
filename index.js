const CheckResult = require('./utils/CheckResult');
const compareArrays = require('./utils/compareArrays');
const regexMatchesToArray = require('./utils/regexMatchesToArray');

module.exports = {
  CheckResult,
  compareArrays,
  regexMatchesToArray,
}